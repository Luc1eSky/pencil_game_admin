import 'package:flutter/material.dart';

class AddAdminButton extends StatelessWidget {
  const AddAdminButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
